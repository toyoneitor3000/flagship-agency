---
description: Alias para el comando /subir. Despliega los cambios a producción.
---

// turbo-all
1. Ejecuta el workflow completo definido en `/subir`. 
   Sigue exactamente los mismos pasos: git status, git add, git commit detallado en español, git push y npm run deploy.
